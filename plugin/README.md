An implementation of the interface for direct feeding of a force into OpenMM.
=============================================================================

You have to install the plugin into your current installation of OpenMM before running examples.